# Regression fixtures

These files contain game-state snapshots used to reproduce movement, combat,
doorway, TNT and switch failures offline. They are not complete run recordings.
Game coordinates, room geometry, entity IDs and seeds are retained so regressions
remain reproducible. No API credentials or provider responses are required.
